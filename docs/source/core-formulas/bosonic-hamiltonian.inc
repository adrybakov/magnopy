.. math::
  \mathcal{H}
  =
  E^{(0)}
  +
  E^{(2)}
  +
  \sum_{\beta}
  \sum_{\boldsymbol{k}}
  \omega_{\beta}(\boldsymbol{k})
  \Biggl(
  b^{\dagger}_{\beta}(\boldsymbol{k})b_{\beta}(\boldsymbol{k})
  +
  \dfrac{1}{2}
  \Biggr)
  +
  \dots
